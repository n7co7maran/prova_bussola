services:
  db:
    image: postgres:latest
    container_name: postgres_cont2
    restart: always
    environment:
      POSTGRES_USER: user            
      POSTGRES_PASSWORD: password  
      POSTGRES_DB: mydb                
    ports:
      - "5432:5432"    